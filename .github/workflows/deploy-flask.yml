name: Flask API CI/CD Pipeline

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Flask workflow disabled
        run: echo "Flask workflow is disabled for now."
